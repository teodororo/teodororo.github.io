# oiiiiiiiiiii

blog pessoal estático feito com **jekyll** (gerador de site estático em Ruby),
hospedado de graça no **github pages**.

## setup

```bash
gem install bundler
bundle install
bundle exec jekyll serve --livereload
```
